from vplotter_motion import (
    setup,
    cleanup,
    move_to,
    MOTOR_SPACING,
)


def draw_big_cat(cx, cy, scale=2.0):
    """
    一筆書きの大きめ猫顔

    scale=1.0:
      横 約300mm
      縦 約220mm

    scale=2.0:
      横 約600mm
      縦 約440mm
    """

    def p(dx, dy):
        return (
            cx + dx * scale,
            cy + dy * scale,
        )

    path = [
        # -------------------------
        # 左耳からスタート
        # -------------------------
        p(-120, -40),
        p(-140, -110),
        p(-75, -75),

        # 頭頂部
        p(-35, -95),
        p(0, -100),
        p(35, -95),

        # 右耳
        p(75, -75),
        p(140, -110),
        p(120, -40),

        # -------------------------
        # 右顔輪郭
        # -------------------------
        p(135, 0),
        p(125, 45),
        p(95, 80),
        p(55, 100),
        p(0, 108),

        # -------------------------
        # 左顔輪郭
        # -------------------------
        p(-55, 100),
        p(-95, 80),
        p(-125, 45),
        p(-135, 0),
        p(-120, -40),

        # -------------------------
        # 左目
        # -------------------------
        p(-85, -20),
        p(-55, -35),
        p(-30, -20),
        p(-55, -8),
        p(-85, -20),

        # 鼻へ
        p(0, 18),

        # -------------------------
        # 右目
        # -------------------------
        p(30, -20),
        p(55, -35),
        p(85, -20),
        p(55, -8),
        p(30, -20),

        # 鼻
        p(0, 18),
        p(-12, 30),
        p(0, 38),
        p(12, 30),
        p(0, 18),

        # -------------------------
        # 口
        # -------------------------
        p(-18, 48),
        p(-5, 58),
        p(0, 48),
        p(5, 58),
        p(18, 48),

        # -------------------------
        # 右ひげ
        # -------------------------
        p(55, 35),
        p(135, 20),
        p(55, 42),
        p(145, 45),
        p(55, 50),
        p(135, 70),

        # 顔中央に戻る
        p(0, 38),

        # -------------------------
        # 左ひげ
        # -------------------------
        p(-55, 35),
        p(-135, 20),
        p(-55, 42),
        p(-145, 45),
        p(-55, 50),
        p(-135, 70),

        # 最後に鼻へ戻る
        p(0, 38),
    ]

    print("Drawing BIG CAT")

    for px, py in path:
        move_to(px, py)


if __name__ == "__main__":
    setup()

    try:
        cx = MOTOR_SPACING / 2.0
        cy = 450.0

        draw_big_cat(
            cx,
            cy,
            scale=1.8,
        )

        print("BIG CAT COMPLETE :3")

    finally:
        cleanup()x